namespace AdvancedPS.Core.Examples
{
    /// <summary>
    /// Addressable popup with <c>LoadMode.Lazy</c>: its prefab loads from Addressables on the first
    /// <c>LayerShow(HUB)</c>, then behaves like any other popup.
    /// </summary>
    public class LazyPopupDemo : AdvancedPopup
    {
    }
}
