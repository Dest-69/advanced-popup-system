---
type: moc
status: active
description: Map of the code documentation — live docs per subsystem. The list is generated automatically from Code.base.
---

# _Code_Map

Live docs per code subsystem. The table below is generated from frontmatter (`Code.base`, `type: code`) —
no need to add a note to the list by hand.

**Mandatory reading before any task:** [[Invariants]], [[Code Style]], [[Project Map]].
**Router for AI agents** ("task → note") — `.agents/rules/advanced-popup-system.md` (at the asset root, next to `.git`).
Before changing a subsystem read its note; after significant changes — update it.

![[Code.base]]
